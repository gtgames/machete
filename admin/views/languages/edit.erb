<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:languages, :index) %></li>
      <li><%= link_to pat(:new), url(:languages, :new) %></li>
      <li class="active"><%= link_to pat(:edit), url(:languages, :edit, :id => @language.id) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:edit) %> <% mt(:language) %></h2>
    <div class="inner">
      <% form_for :language, url(:languages, :update, :id => @language.id), :method => :put, :class => :form do |f| %>
        <%= partial "languages/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>