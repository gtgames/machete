<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:menus, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:menus, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:menu) %></h2>
    <div class="inner">
      <% form_for :menu, url(:menus, :create), :charset => 'utf8', :class => :form do |f| %>
        <%= partial "menus/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>