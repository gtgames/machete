<% form_for :configuration, url(:configurations, :update, :id => @configuration.id), :method => :put, :class => :form do |f| %>
  <%= partial "configurations/form", :locals => { :f => f } %>
<% end %>

<% content_for :actions, partial("base/actions", :locals => { :p => :configurations }) %>

<% content_for :title, 'Modifica Chiave di configurazione' %>
