<table cellspacing="0">
  <thead>
    <tr>
      <th>&nbsp;</th>
      <th><%= mat(:taxonomy, :title) %></th>
      <th><%= mat(:taxonomy, :permalink) %></th>
      <th class="actions"></th>
    </tr>
  </thead>
  <tbody>
    <% @taxonomy.each do |tax| %>
      <tr>
        <td><input type="checkbox"></td>
        <td><%= tax.title %></td>
        <td><%= tax.url %></td>
        <td>
          <%= button_to pat(:edit), url(:links, :edit, :id => tax.id), :method => :get, :class => 'button_to edit' %>
          <%= button_to pat(:delete), url(:links, :destroy, :id => tax.id), :method => :delete, :class => 'button_to trash' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<% content_for :actions, partial("base/actions", :locals => { :p => :taxonomy }) %>
<% content_for :title, 'Tassonomia' %>
