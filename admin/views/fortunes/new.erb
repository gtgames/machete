<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:fortunes, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:fortunes, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:fortune) %></h2>
    <div class="inner">
      <% form_for :fortune, url(:fortunes, :create), :charset => 'utf8', :class => :form do |f| %>
        <%= partial "fortunes/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>