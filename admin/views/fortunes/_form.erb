<div class="group">
  <%= f.label :fortune %><%= f.error_message_on :fortune %>
  <%= f.text_field :fortune, :class => :text_field %>
  <span class="description">Testo detto popolare</span>
</div>

<div class="group navform wat-cf">
  <%= f.submit pat(:save), :class => :button %>
  <%= f.submit pat(:cancel), :onclick => "window.location='#{url(:fortunes, :index)}';return false", :class => :button %>
</div>
