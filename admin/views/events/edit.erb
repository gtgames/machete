<% form_for :event, url(:events, :update, :id => @event.id), :method => :put, :class => :form do |f| %>
  <%= partial "events/form", :locals => { :f => f } %>
<% end %>

<% content_for :actions, partial("base/actions", :locals => { :p => :events }) %>

<% content_for :title, 'Modifica Evento' %>
