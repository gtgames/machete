%nav.secondary-navigation
  %ul
    %li.first=link_to pat(:list), url(:partner, :index)
    %li=link_to pat(:new), url(:partner, :new)
    %li.active=link_to pat(:edit), url(:partner, :edit, :id => @partner.id)
#content
  %h2
    =pat(:edit)
    =mt(:photo)
  .inner
    -form_for :photo, url(:partner, :update, :id => @partner.id), :method => :put, :multipart => true, :class => :form do |f|
      =partial "partner/form", :locals => { :f => f }
