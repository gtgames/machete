<table>
  <thead>
    <tr>
      <th>&nbsp;</th>
      <th><%= mat(:post, :title) %></th>
      <th><%= mat(:post, :tag_list) %></th>
      <th class="actions"></th>
    </tr>
  </thead>
  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><input type="checkbox"></td>
        <td><%= post.title %></td>
        <td><%= post.tag_list %></td>
        <td>
          <%= button_to pat(:edit), url(:posts, :edit, :id => post.id), :method => :get, :class => 'button_to edit' %>
          <%= button_to pat(:delete), url(:posts, :destroy, :id => post.id), :method => :delete, :class => 'button_to trash' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<% content_for :actions, partial("base/actions", :locals => { :p => :posts }) %>

<% content_for :title, 'Articoli' %>
