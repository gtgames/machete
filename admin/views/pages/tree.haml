%nav.secondary-navigation
  %ul
    %li.first=link_to pat(:list), url(:pages, :index)
    %li=link_to pat(:new), url(:pages, :new)
    %li.active=link_to pat(:tree), url(:pages, :tree)
#content
  %h2
    =pat(:all)
    =mt(:page)
  .inner
    #tree= tree


  -form_for :page, url(:pages, :tree), :method => :post, :class => :form do |f|
    = f.hidden_field :serialized
    = f.submit pat(:save), :class => :button, :id => :save
    = f.submit pat(:cancel), :onclick => "window.location='#{url(:pages, :index)}';return false", :class => :button
