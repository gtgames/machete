<table cellspacing="0">
  <thead>
    <tr>
      <th>&nbsp;</th>
      <th class="sortable"><%= mat(:taxonomy, :title) %></th>
      <th class="sortable"><%= mat(:taxonomy, :permalink) %></th>
      <th class="actions">&nbsp;</th>
    </tr>
  </thead>
  <tbody>
    <% @taxonomy.each do |tax| %>
      <tr>
        <td><input type="checkbox"></td>
        <td><%= tax.title %></td>
        <td><%= tax.path %></td>
        <td>
          <%= button_to pat(:edit), url(:taxonomy, :edit, :id => tax.id), :method => :get, :class => 'button_to edit' %>
          <%= button_to pat(:delete), url(:taxonomy, :destroy, :id => tax.id), :method => :delete, :class => 'button_to trash', :onsubmit => "return confirm('#{pat(:confirm)}')" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<% content_for :actions, partial("base/actions", :locals => { :p => :taxonomy }) %>
<% content_for :title, 'Tassonomia' %>
