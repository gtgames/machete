<% form_for :page, url(:pages, :update, :id => @page.id), :method => :put, :class => :form do |f| %>
  <%= partial "pages/form", :locals => { :f => f } %>
<% end %>

<% content_for :actions, partial("base/actions", :locals => { :p => :pages }) %>

<% content_for :title, 'Modifica Documento' %>
