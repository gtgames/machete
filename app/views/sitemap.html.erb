<% @posts.each do |post| %>
  <a href="http://<%= Cfg[:domain] %>/<%= url(:posts, :show, :slug => post.slug) %>"><%= post.title %></a>
<% end %>
<% @pages.each do |page| %>
  <a href="http://<%= Cfg[:domain] %>/<%= url(:pages, :show, :slug => page.slug) %>"><%= page.title %></a>
<% end %>
