<table class="table table-striped">
  <thead>
    <tr>
      <th class="sortable"><%= mat(:hotel, :title) %></th>
      <th class="sortable"><%= mat(:hotel, :address) %></th>
      <th class="last">&nbsp;</th>
    </tr>
  </thead>
  <tbody>
    <% @hotels.each do |hotel| %>
    <tr>
      <td><%= hotel.title %></td>
      <td><%= hotel.address %></td>
      <td>
        <%= edit_button pat(:edit), url(:cerca_hotel, :edit, :id => hotel.id) %>
        <%= delete_button pat(:delete), url(:cerca_hotel, :destroy, :id => hotel.id) %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>


<% content_for :actions, partial("admin/base/actions", :locals => { :p => :cerca_hotel }) %>

<% content_for :title, 'Hotel' %>
